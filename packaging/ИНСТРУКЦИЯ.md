# Как запустить «Стратегическую игру»

1. Распакуйте архив — появится «Стратегическая игра.app».
2. **Первый запуск:** нажмите на приложение правой кнопкой мыши, выберите
   «Открыть», затем в появившемся окне ещё раз «Открыть».
   Обычный двойной клик в первый раз не сработает: macOS блокирует программы
   без подписи разработчика. После первого раза приложение открывается как
   обычно.
3. Откроется браузер со стартовой страницей. Выберите сценарий и нажмите
   «Начать».

## Команды со своих телефонов

Запустите приложение и на стартовой странице выберите сценарий. Если команды
должны заходить со своих телефонов, приложение нужно запустить в сетевом
режиме — тогда на пульте появится адрес вида `192.168.0.200:8000` и QR-код для
каждой команды. Телефоны и компьютер должны быть в одной сети Wi-Fi.

Важно понимать: в сетевом режиме приложение видно всем, кто в этой сети.
Защита — только код команды, поэтому в этом режиме коды длиннее и после трёх
неверных попыток вход для этой команды приостанавливается. Не запускайте
сетевой режим в открытой сети, где вам не всё равно, кто увидит брифинги.

## Как проходит занятие

- Ведущий держит открытым **пульт** (главная страница) и, если есть проектор,
  вкладку **«Экран для проектора»**.
- Пульт показывает, какой команде передать компьютер, и код каждой команды.
- Команда открывает свою страницу, вводит код, выбирает приказы и нажимает
  «Сдать приказы». Экран сразу закрывается. Пока команда думает, экран можно
  погасить кнопкой «Скрыть экран»; сам он гаснет через минуту бездействия.
- Когда сдали все, ведущий нажимает «Закрыть раунд». Если время поджимает —
  «Закрыть принудительно»: несдавшие команды пасуют.
- Ошиблись — «Откатить раунд» возвращает всё как было.
- После последнего раунда пульт предлагает перейти к разбору.

## Где лежат данные

`~/Library/Application Support/StrategicGame/`
— `sessions/` сохранённые партии, `scenarios/` ваши сценарии.
Файл партии самодостаточен: в нём лежит и текст сценария, поэтому его можно
переслать коллеге одним файлом.

## Свои сценарии

Положите файл `.yaml` в `scenarios/` — он появится в списке при создании
партии. Если файл написан с ошибкой, он просто не попадёт в список: проверить
его и увидеть номера строк с ошибками можно командой

```
python3 -m sgame validate мой-сценарий.yaml
```

(из папки с исходным кодом; см. README).
